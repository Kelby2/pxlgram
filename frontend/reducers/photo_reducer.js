import { RECEIVE_ALL_PHOTOS,
        RECEIVE_PHOTO,
        RECEIVE_PHOTOS } from '../actions/photo_actions';

const PhotoReducer = (initialState = {}, action) => {
  Object.freeze(initialState);
  let newState;

  switch (action.type) {
    
  }
}
